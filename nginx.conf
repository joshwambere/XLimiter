http {

    upstream all {
        server host.docker.internal:1000;
        server host.docker.internal:5050;
    }

    server {
         listen 8080;
         location / {
              proxy_pass http://all/;
         }
    }

}


events { }
